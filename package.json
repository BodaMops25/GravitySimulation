{
  "name": "gravitysimulation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --minify --sourcemap --outfile=./dist/bundle.js"
  },
  "author": "BodaMops25",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
